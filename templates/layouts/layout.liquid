<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{% block title %}Mixture + Bourbon{% endblock %}</title>
        {{ "your-project.css" | asset_url | stylesheet_tag }}
        <!-- TODO: Complete this project :) -->
    </head>
    <body class="wrapper">
        {% include "navigation" %}
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </body>
</html>